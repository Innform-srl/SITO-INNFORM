<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- Background with rounded corners -->
  <rect width="100" height="100" rx="16" fill="#fff"/>

  <!-- Three bars from the Innform logo -->
  <!-- Pink/Magenta bar (i) -->
  <rect x="12" y="10" width="22" height="80" rx="4" fill="#C66B9A"/>

  <!-- Light blue bar (first n) -->
  <rect x="39" y="10" width="22" height="80" rx="4" fill="#5BAFCB"/>

  <!-- Darker blue bar (second n) -->
  <rect x="66" y="10" width="22" height="80" rx="4" fill="#3B8BA8"/>
</svg>
